# Weather app

with react and vite
auto build 시도중

#### Available Scripts

```node
yarn start
yarn build
yarn serve
```

#### package.json

```json
"scripts": {
    "start": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  }
```
